[Unit]
Description=VS Code Daily Settings Sync Timer
Requires=vscode-daily-settings-sync.service

[Timer]
# Run once per day at 6 PM
OnCalendar=18:00
# Also run 15 minutes after boot if the machine was off at scheduled time
Persistent=true

[Install]
WantedBy=timers.target
